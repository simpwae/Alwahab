# Alwahab — Handoff

## 1. Goal

Build out the Alwahab storefront beyond the customer-facing base (Phases 1–4): **Phase 5 (Auth + Customer Account)**, then the **Admin Panel** (project docs' "Phases 6–10"). Requested phase-by-phase / section-by-section so progress is trackable one step at a time. Mock-only for now (localStorage + in-memory React context, no backend) — the user's stated end goal is a real backend on **Next.js + Supabase**, but that's a separate future migration, done after the UI is proven.

## 2. Current state

**Phase 5 (Auth + Customer Account)** is complete: mock login/signup, protected `/account/*` routes, a real (non-fake) wishlist, order history, address book, and profile editing.

**Admin Panel** is complete, built in two passes:
- **Shell**: `/admin/login`, protected `/admin` dashboard with real stat tiles, dark sidebar shell.
- **Six follow-up sections**, each fully wired to shared React contexts (not disconnected admin-only state) so admin actions are real: deleting a product removes it from storefront listings, approving a review makes it appear on the PDP, editing shipping settings changes Cart/Checkout math.
  - **Orders** — list + detail with fulfillment/payment status and tracking-number editing.
  - **Products** — list + add/edit form. New `ProductContext` replaced the static `sampleProducts` import across 6 storefront consumers (Home, CategoryListing, SearchResults, ProductDetail, AccountWishlist, Header search).
  - **Customers** — read-only list + detail with order history.
  - **Coupons** — list + add/edit form. New `CouponContext`; `findValidCoupon` now takes the coupon list as a parameter instead of closing over the static export; Cart wired to it.
  - **Reviews** — moderation list with inline Approve/Reject. New `ReviewContext`; ProductDetail's approved-reviews display now reads from it.
  - **Settings** — bank-transfer + shipping form. New `StoreSettingsContext`; BankTransferPanel, Checkout, and Cart all read from it instead of the static `storeSettings` export.

**Responsive/styling pass** done on the admin panel afterward (the newest, least-tested part — the customer storefront from Phases 1–4 already followed the token/breakpoint conventions consistently). First iteration: the admin sidebar was a full-height dark block stacking above all content on mobile; changed to a horizontal icon bar. Also: semantic color-coding on dashboard stat tiles (amber/red for "needs attention" tiles vs. teal/blue for neutral ones), progressive column-hiding on the Products/Coupons tables instead of forced horizontal scroll, and larger touch targets on icon buttons. Verified with actual Playwright screenshots (see §5) across mobile/tablet/desktop — caught and fixed one real bug (Coupons table's delete icon pushed off-screen at 390px width).

**Sidebar, second iteration**: the horizontal icon-bar approach itself turned out to be broken — user reported it after deploying, and measuring the DOM confirmed the sidebar's content was 486px wide against a ~375–428px mobile viewport, so Reviews, Settings, and Log Out were scrolled off-screen with **no visual indication they existed**. Replaced with a hamburger + slide-out drawer on mobile (full labeled nav list, closes on navigate), reusing the exact pattern `Header.tsx` already uses for the storefront's mobile menu (same framer-motion animation). Desktop sidebar unchanged, confirmed pixel-identical via screenshot.

**Vercel deployment fix**: user reported the admin panel (and, implicitly, any deep link) 404'd on the live Vercel deployment despite the storefront homepage working. Root cause: this is a client-side-routed SPA (`react-router-dom` `BrowserRouter`), and `dist/` only ever contains `index.html` — there was no `vercel.json`, so Vercel's static host had no rule to serve `index.html` for unknown paths and 404'd on any direct visit/refresh to anything but `/`. Added `vercel.json` with a catch-all rewrite to `index.html`. **Not yet confirmed against the actual live Vercel deployment** — verified locally by inspecting `dist/` output (confirms the failure mode) and by reasoning about Vercel's rewrite behavior (standard, well-known fix), but no access to the live URL from this environment to click-test it post-deploy.

**Follow-up feature batch** (separate session, after the above): the user asked for 5 things before moving to Next Steps. Four were built, one turned out to already exist:
- **Sales analytics** — added `recharts` (first charting dependency in the repo) and a "Sales Overview" card on `/admin` (`AdminDashboard.tsx`) with a Daily/Monthly toggle, grouping `OrderContext`'s `orders` by `date` or `date.slice(0,7)` and summing `total`. No new context/type needed — it's a derived view over existing order data.
- **Product discounts** — new `ProductContext.applyDiscount(ids, discountPct)` (recomputes `sellingPrice`/`discountPct` off `originalPrice`) and a new `/admin/discounts` page (`AdminDiscounts.tsx`) with three scopes — Single Product, Bulk Select (checkboxes), Category (dropdown, using a `CATEGORIES` list hoisted out of `AdminProductForm.tsx` into `src/data/categories.ts` so both pages share it). All three funnel into the same `applyDiscount` call; storefront sale badges/PDP strikethrough already read `sellingPrice`/`discountPct` off `ProductContext`, so nothing downstream needed touching.
- **Customer details on orders** — this was a **display gap, not a data gap**: `Order` already carried `phone` and `shippingAddress` (set at checkout) but neither `AdminOrders.tsx` (list) nor `AdminOrderDetail.tsx` (detail header) rendered them. Both now do.
- **Required checkout fields** (name/phone/address/city) — user asked for this expecting it might be missing; it was already fully enforced (`Checkout.tsx`'s `validateContact()` and `AccountAddresses.tsx`'s own required-field check both already existed). No code changed for this one, confirmed with the user instead.
- **Account sidebar mobile background bug** — `AccountNav.tsx`'s `<nav>` had `border` + `rounded-2xl` but no background class at all, so it was transparent at every breakpoint (most visible on mobile, sitting above scrollable page content). One-line fix: added `bg-white`.

Verified all five live via Playwright (see §5) rather than just by reading the diff — caught nothing broken, but confirmed the discount write actually propagates through `ProductContext` end-to-end (headphones PKR 12,999→10,399 after a 20% Electronics-category discount, matching `originalPrice × 0.8`, visible on the Products list after an in-app client-side nav).

`npm run lint` is clean relative to baseline (2 pre-existing errors — `BankTransferPanel.tsx` empty arrow function, `CartContext.tsx` inferrable-type — same as before all this work, no new ones introduced).

`/admin/<anything not yet listed above>` returning 404 (client-side, via the app's own `NotFound` catch-all route) is expected.

**Second follow-up batch** (same "next steps" list, user picked 3 of 4 options — skipped the Phase 6/Next.js+Supabase migration, correctly treating it as its own future planning effort rather than something to fold into a quick pass):
- **Admin bulk delete + pagination + delete-guard**, on both Products and Coupons — a header "select all" checkbox plus per-row checkboxes, a "Delete Selected" bar, an 8-per-page "Load More" (reusing the same slice-based accumulator pattern already duplicated between `CategoryListing.tsx`/`SearchResults.tsx`, not a new pagination component), and a guard that blocks deleting anything referenced by an existing order (`OrderItem.productId` / `Order.couponCode`) — single-item delete blocks outright with a toast; bulk delete skips just the referenced ones and reports "N deleted, M skipped" so the whole batch doesn't fail over one blocked item. New `ProductContext.deleteProducts(ids)` / `CouponContext.deleteCoupons(codes)` alongside the existing single-item deletes.
- **Mock password-change UI** on `AccountProfile.tsx` only (a second form: current/new/confirm password, min-8-chars + match validation, toast on submit) — deliberately *not* added to the admin side, since there's no admin "my profile" page today (`AdminSettings.tsx` is store-wide config, not a personal account page); add one if an admin profile page gets built. Purely cosmetic per the mock architecture: `User`/`Admin` types have no password field and `login()` doesn't check one, so nothing is actually persisted — matches how the rest of "auth" already works here.
- **Test suite**: added `vitest` (zero framework decision needed — it's the Vite-native test runner, reuses the existing Vite config/transform pipeline) via `vite.config.ts`'s `test` block, `npm run test` script. Deliberately skipped `jsdom`/`@testing-library/react` — every test target is a pure function, no rendering needed. 26 tests across 4 files: `src/data/sampleCoupons.test.ts` and `src/utils/productFilters.test.ts` needed zero source changes (already-exported pure functions); `src/pages/admin/AdminDashboard.test.ts` and `src/pages/Checkout.test.ts` needed `groupSales`/`formatBucketLabel`/`validateContact`/`ContactShippingValues` promoted from module-private to `export` (no logic changes). **Writing the `formatBucketLabel` test caught a real timezone bug**: it parsed a date-only string (`"2026-06-05"`) with `new Date(dateString)`, which JS treats as UTC midnight, then formatted in local time — on a machine west of UTC this silently shows the wrong day on the Sales Overview chart. Fixed by parsing the `Y-M-D` components manually and constructing the `Date` with local-time arguments (`new Date(year, month-1, day)`), which sidesteps the UTC round-trip entirely. Worth remembering: any other spot in this codebase that does `new Date(dateOnlyString)` has the same latent bug.

**Phase 6, Wave 1 (Next.js scaffold + public-storefront parity)** — the user said "let's start it" on the long-deferred Next.js + Supabase migration. Broke it into sub-phases (6a routing/framework parity → 6b Supabase schema+auth → 6c rewire each context → 6d cutover) and, within 6a, into waves matching every other phase in this project's history (small, requestable slices). This session built **Wave 1 only**: a brand-new, fully self-contained Next.js 16 (App Router) app at `next-app/` — own `package.json`/`node_modules`, sibling to `src/`, **not touching the existing Vite app at all** — covering the 13 public storefront routes (Home, style guide, category listing, deals, search, product detail, cart, checkout, order confirmation, track order, login, signup, 404) plus all 9 context providers and every shared component they depend on. Data is still 100% mock (same sample-data files, same localStorage-backed pseudo-auth) — this wave proves the *framework* migration in isolation before Supabase enters the picture at all. `/account/*` (Wave 2) and `/admin/*` (Wave 3) are **not built yet** in `next-app/`.

The scaffold landed on **Next.js 16.2.10 / React 19.2 / Tailwind v4** (current `create-next-app` defaults) rather than matching the old app's Vite/React 18/Tailwind v3 versions exactly — Next 16 ships its own `AGENTS.md` warning that it has real breaking changes from older training-data assumptions, so its bundled docs (`node_modules/next/dist/docs/`) were read before writing App Router code rather than assumed. Consequences worth knowing:
- Tailwind v4 uses CSS-first config (`@theme` block in `globals.css`), not `tailwind.config.js` — the same color/radius/shadow/font tokens were re-declared there, not copy-pasted as a JS file.
- All 34+ ported files are Client Components (`"use client"`) for now, uniformly — no Server Component adoption in this wave, deliberately (stated in the plan as a later optimization, not a parity requirement).
- Router API swap throughout: `react-router-dom`'s `Link`/`useNavigate`/`useParams`/`useSearchParams` → `next/link`'s `Link`/`next/navigation`'s `useRouter`/`useParams`/`useSearchParams`. Pages using `useSearchParams` (`/search`, `/track-order`, `/category/[slug]`, `/deals`) are wrapped in `<Suspense>` at the route level, per Next's requirement.
- **`navigate(path, { state })` has no Next.js equivalent** — react-router's location-state passing had to be redesigned, not just swapped: `CartContext` gained an `appliedCode`/`setAppliedCode` field so the coupon applied on `/cart` survives the navigation to `/checkout` (Checkout now recomputes the discount itself from `appliedCode` + `subtotal` + `coupons` instead of trusting a passed-in number — arguably *more* correct than the original). The Checkout→OrderConfirmation state-passing was dropped entirely since `OrderConfirmation` already had a `getOrderById(orderId)` fallback that's sufficient on its own (the state was only ever a same-tick optimization).

**Three real, pre-existing bugs surfaced during the port** (all silently present in the original Vite app too, just never caught because `vite build` never runs `tsc`, and this migration's stricter Next.js/TypeScript build does):
1. Roughly two dozen files reference the bare type `ReactNode` (or `FormEvent`, `ButtonHTMLAttributes`, etc.) without importing it from `react` — works under Vite's untyped esbuild transform, hard TypeScript error under `next build`. Fixed by adding the missing named imports everywhere it appeared (no logic changes).
2. `Header.tsx`'s search-suggestions dropdown attaches `useRef<HTMLDivElement>` to a `<form>` element — a real type mismatch, caught by `next build`'s type-check. Fixed by correcting the ref's generic type to `HTMLFormElement`.
3. **`AuthContext`/`AdminAuthContext` read `localStorage` synchronously as their `useState` initializer.** Next.js server-renders Client Components too (for the initial HTML, both at build-time static generation and at request time) — `localStorage` doesn't exist during that server pass, so this crashed the build outright (`ReferenceError: localStorage is not defined`). Fixed by initializing both to `null`/`null` on first render (matches server and client exactly, avoiding a hydration mismatch) and moving the actual `localStorage` read into a `useEffect` that only fires post-mount in the browser. Same root cause independently caught a second time in `FlashDeals.tsx`'s countdown timer (`Date.now()` computed during the initial render differs between the server pass and the client hydration pass by design, producing a real "server text didn't match client" hydration-mismatch console error on `/`) — fixed the same way, deferring the `Date.now()`-dependent state to `useEffect`.

Verified with the same scratchpad Playwright setup as prior sessions: screenshotted all 13 routes side-by-side against the still-running old Vite app (visually identical, module the hero carousel's independent auto-rotate timer), then ran a full click-through flow on the new app only — browse → add to cart → apply a coupon on `/cart` → checkout → place a COD order → land on `/order-confirmation/[id]` with the correct discount/total/address → click through to `/track-order` and find the same order by ID+email with the right status timeline. Zero console errors on the final run (the one hydration mismatch and one `localStorage` crash both surfaced *during* this verification pass and got fixed before it passed clean). `next build`, `npx eslint .`, and `npx vitest run` (ported the 3 relevant pure-function test files — `sampleCoupons.test.ts`, `productFilters.test.ts`, and a `Checkout` test now colocated as `app/checkout/page.test.ts`) are all clean; one new ESLint default (`react-hooks/set-state-in-effect`, part of `eslint-config-next` 16) was disabled repo-wide in `next-app/eslint.config.mjs` with an explanatory comment, since it blanket-flags the "reset state when a route param changes" and "read a browser API once after mount" patterns used throughout — both legitimate here, not bugs.

**Phase 6, Wave 2 (customer `/account/*` parity)** — same session, immediately after Wave 1. Ported the 6 protected customer routes (`AccountDashboard`, `AccountOrders`, `AccountOrderDetail`, `AccountWishlist`, `AccountAddresses`, `AccountProfile` — the last one carries the mock password-change form added earlier this session) plus `AccountLayout`/`AccountNav`/`ProtectedRoute`, same mechanical pattern as Wave 1 (`"use client"`, `next/link`/`next/navigation` swaps).

Two things needed actual design work, not just mechanical porting:
- **`ProtectedRoute` needed an `isInitialized` flag added to `AuthContext`.** Wave 1's SSR-safety fix means `user` always starts `null` on every render (real auth state only arrives after a post-mount `useEffect`). Without a way to distinguish "still checking" from "confirmed logged out," a naive port of `ProtectedRoute` would redirect every visitor to `/login` for a frame before the real session loads. `isInitialized` (false until that first effect resolves) fixes it: `ProtectedRoute` renders nothing until initialized, then decides.
- **Logging out from a protected page raced two navigations and lost.** `AccountNav`'s logout button calls `logout()` then navigates home; `ProtectedRoute`'s effect *also* reacts to `user` becoming `null` and redirects to `/login`. In the old react-router app this was never a problem (`navigate()` swaps the rendered route tree synchronously, so the old page's `ProtectedRoute` is already gone by the time state settles). Next.js's App Router navigation is transition-based, not synchronous, so both redirects were live at once — and `/login` was winning, landing users on the login page immediately after logging out instead of on the homepage. Fixed by making logout a hard navigation (`window.location.href = '/'` after `logout()`, which synchronously clears `localStorage` before the reload) — sidesteps the race entirely and, as a bonus, guarantees every other context resets to its default state too, which is arguably what "log out" should mean anyway.

Verified the same way as Wave 1: `next build` (17 routes now — 4 dynamic), `eslint` (0 errors), `vitest` (unchanged, 21 tests — Wave 2 added no new pure-function test surface), and a full Playwright pass: unauthenticated `/account` correctly bounces to `/login`; log in; walk Dashboard → Orders → Order Detail → Wishlist → Addresses (added a real address, persisted) → Profile; log out; confirm `/account/orders` is protected again post-logout. Zero console errors on the final run — the logout race above was caught and fixed *during* this verification pass, same as Wave 1's SSR bugs.

**Phase 6, Wave 3 (`/admin/*` parity, 15 routes)** — same project, new session, picking up exactly where the prior handoff's Next Steps pointed. Ported all 15 admin routes into `next-app/`: `/admin/login`, `/admin` (dashboard + Sales Overview chart), `/admin/orders` (+ `/admin/orders/[id]`), `/admin/products` (+ `/admin/products/new` and `/admin/products/[id]/edit`), `/admin/discounts`, `/admin/customers` (+ `/admin/customers/[id]`), `/admin/coupons` (+ `/admin/coupons/new` and `/admin/coupons/[code]/edit`), `/admin/reviews`, `/admin/settings`. Same mechanical pattern as Waves 1–2 (`"use client"`, `react-router-dom` → `next/link`/`next/navigation` swaps, thin `page.tsx` wrappers around a component defined in the same file). `recharts` added to `next-app/package.json` — Waves 1–2 never needed it since the Sales Overview chart only lives on the admin dashboard. `AdminDashboard`'s `groupSales`/`formatBucketLabel` exported from `app/admin/page.tsx` and a colocated `page.test.ts` ported too, matching the `Checkout` test's precedent from Wave 1 — same 26 tests as the old app.

Two components — `AdminProductForm` and `AdminCouponForm` — are each used by two URL patterns (`/new` and `/[id]/edit` or `/[code]/edit`). Followed the established `screens/` convention from Wave 1's `CategoryListingScreen` (shared across `/category/[slug]` and `/deals`) instead of duplicating the form: `src/screens/AdminProductFormScreen.tsx` and `AdminCouponFormScreen.tsx`, each reading `useParams()` itself (empty on the `/new` route, populated on the `/edit` route — exactly mirrors the old app's single-component-branches-on-`Boolean(id)` pattern), with two one-line `page.tsx` files per form just importing and rendering the screen.

**The `isInitialized` gap flagged in the prior handoff's Next Steps was real and exactly as described**: `next-app/src/context/AdminAuthContext.tsx` had been ported in Wave 1 (as one of "all 9 context providers") but never got the SSR-safety fix `AuthContext` received in Wave 2 — `admin` always started `null` on first render with no way to tell "still checking localStorage" from "confirmed logged out." Fixed the same way as `AuthContext`: added `isInitialized`, set `true` inside the post-mount `useEffect`. `AdminProtectedRoute` and `AdminSidebar`'s logout handler were ported directly off `ProtectedRoute`/`AccountNav` (including the hard-navigation-on-logout fix from Wave 2, applied here pre-emptively rather than discovered again by a race — `window.location.href = '/admin/login'` after `logout()`, not `router.push`).

Verified with `next build` (32 routes total now — up from 17 after Wave 2, 15 new under `/admin/*`), `npx tsc --noEmit` (clean), `npx eslint .` (0 errors — the only warnings are the same pre-existing `<img>`/`no-img-element` ones from Waves 1–2, none new), `npx vitest run` (26/26 passed), and a full Playwright pass against `next dev`: unauthenticated `/admin` → `/admin/login`; log in; walk all 8 sidebar sections; open an order and change its fulfillment status; edit a product and save; open the empty "Add Product" form and cancel; apply a category-wide discount and confirm it's reflected on the Products list without leaving the client-side session; open a customer detail page; create-then-cancel and edit-then-save a coupon; approve a review (or confirm none pending); save store settings; log out from a protected page and confirm it lands on `/admin/login` (not stuck — the race this session pre-emptively guarded against); confirm `/admin/orders` is protected again post-logout. Zero console errors on the final run.

Both Wave 1+2 and Wave 3 are **committed**. **Not pushed** — no push requested yet.

**`/admin/*` (Wave 3) is done.** All of 6a (routing/framework parity: 32 routes across public storefront, customer account, and admin) is now complete on mock data.

**Phase 6b (Supabase schema + auth)** — same session, immediately after Wave 3 was pushed. The user created the Supabase project themselves (`syskdhrzcrvaligqirvg`, region `ap-south-1`, Postgres 17) and pasted the API keys + DB password directly into chat. **Flagged that as a real exposure path and recommended rotating the service_role key + DB password once this phase settled** — pasting secrets into a chat log isn't the same as an env file, even in a private session. The CLI (`supabase` installed via `npm install supabase --save-dev`, invoked through `npx` — it deliberately has no global-install path) had been installed at the repo root by the user; moved it into `next-app/` instead, since that's the only app that will ever talk to Supabase (the old Vite app is being retired, not migrated). Ran `supabase init` and `supabase link --project-ref <ref> --password <pw>` from there; `next-app/supabase/config.toml` now exists, and the real credentials live in `next-app/.env.local` (confirmed gitignored via `next-app/.gitignore`'s `.env*` pattern before writing anything to it).

This phase was planned explicitly (plan mode, user-approved) before any code was written, scoped deliberately narrow: **schema + RLS + seed data + Auth user creation only — zero changes to any React component or context.** The running app's behavior is unchanged; this is pure backend infrastructure, reviewable and revertable on its own, landing before 6c (rewiring the 9 contexts to actually call Supabase) touches a single `.tsx` file. Delivered:
- **`supabase/migrations/20260714143349_initial_schema.sql`** — 10 tables mirroring `next-app/src/types/index.ts` (`profiles`, `addresses`, `admins`, `products`, `coupons`, `orders`, `order_items`, `reviews`, `wishlist_items`, `store_settings`), RLS enabled on all of them, an `is_admin()` `SECURITY DEFINER` helper reused across every admin-gated policy instead of repeating the subquery per table, a `handle_new_user()` trigger that auto-creates a `profiles` row on signup, and a `find_guest_order(order_id, email)` `SECURITY DEFINER` RPC (granted to `anon`) that replicates `OrderContext.findOrder()`'s case-insensitive/trimmed match so `/track-order` keeps working without requiring login. `admins` is kept as its own table (not a role column on `profiles`) to mirror the existing `Admin`/`User` split. Cart was deliberately left out — genuinely ephemeral pre-checkout state, no table needed.
- **`supabase/seed.sql`** — the existing `sampleProducts`/`sampleCoupons`/`sampleOrders`/`sampleReviews`/`storeSettings` ported directly into `insert` statements (same ids/values). Seed orders are left with `user_id = null` (they work as guest/demo orders via `find_guest_order()`; 6c's real checkout will set `user_id` for orders placed by a logged-in customer going forward) — this sidesteps a real seeding-order dependency: `auth.users` doesn't exist yet when `seed.sql` runs, so there was no `profiles.id` to link orders to at that point anyway.
- **`next-app/scripts/seed-auth.mjs`** — a one-off script (not a migration, run once manually), since `auth.users` can't be seeded through `seed.sql` against a *hosted* Supabase project (that only works for local Docker Supabase) — uses the `service_role` key + `supabase.auth.admin.createUser()` to create the admin and 3 seeded customers, all sharing one demo password (`Alwahab123!`, printed at the end of the script — this is mock/demo-phase data, not meant to survive into real usage), then inserts their `addresses` rows and the admin's `admins` row (the `profiles` rows come for free via the trigger).
- **`src/lib/supabase/client.ts`** + **`server.ts`** — thin `@supabase/ssr`-based browser/server client factories, the first `lib/` folder in `next-app/`. **Nothing imports these yet** — that's 6c.

Applied via `npx supabase db push` (migration) then `npx supabase db push --include-seed` (seed.sql — `db push` doesn't apply seed data by default). Verified with a 14-check ad-hoc Node script run from the scratchpad against the live hosted project using the anon key: public read works on `products`/`coupons`/`store_settings`; an anon `update` on `products` is silently filtered to 0 rows by RLS (not an error — worth remembering, Postgres RLS returns success-with-nothing-affected for a blocked UPDATE, not a thrown error, which tripped up the first version of the check); `admins` and `orders` are correctly invisible to anon; `find_guest_order` returns the right order for a correct id+email pair and nothing for a wrong email; reviews only show `Approved` rows; both the seeded admin and a seeded customer can `signInWithPassword`, and the signed-in customer's `profiles` row (name populated correctly from `user_metadata` via the trigger) and seeded address are both visible under their own session. `npx tsc --noEmit`, `npx eslint .` (0 errors, same 17 pre-existing `<img>` warnings), and `next build` (32 routes, unchanged — nothing new is wired into the app yet) all pass clean.

**Phase 6c, Wave 1 (Auth)** — same session, immediately after 6b. The user asked to keep going ("do the next phase... handle it like [an e-commerce site] and proceed") rather than pause for more planning, so this wave was scoped and executed directly rather than re-entering plan mode: `AuthContext` + `AdminAuthContext` (and every direct consumer) rewired to call real Supabase Auth, since every other table's RLS depends on `auth.uid()` being real — the necessary foundation before any other context can be rewired. `ProductContext`/`CouponContext`/`ReviewContext`/`StoreSettingsContext`/`OrderContext`/`WishlistContext` are **still mock** — deliberately out of scope for this wave, next up whenever requested.

Both `AuthContext` and `AdminAuthContext` now use `createClient(storageKey)` from `src/lib/supabase/client.ts` (rewritten from a `@supabase/ssr` cookie-syncing client to plain `@supabase/supabase-js`, since the whole app is still Client Components — nothing server-side reads auth yet) with **two distinct storage keys** (`alwahab-customer-auth` / `alwahab-admin-auth`), so a customer and an admin can hold independent sessions in the same browser at once, same as the old `alwahab_user`/`alwahab_admin` localStorage split. `isInitialized` now tracks whether the first `supabase.auth.getSession()` call has resolved (Supabase manages its own session persistence, so the old manual-localStorage dance is gone); `onAuthStateChange` keeps `user`/`admin` in sync afterward.

`login`/`signup` are now real and password-based (`supabase.auth.signInWithPassword`/`signUp`), returning `string | null` (error message or success) instead of the old synchronous `boolean`. `AdminAuthContext.login` does a second check after authenticating — being a valid Supabase Auth user isn't being an admin, so it looks the signed-in uid up in the `admins` table (RLS-gated to `is_admin()`) and signs back out if it's not there, same separation the mock version had via two disconnected arrays. `AccountProfile`'s password-change form (mock/cosmetic since Phase 5) is now wired to a real `changePassword` (`supabase.auth.updateUser({password})`) — dropped the "Current Password" field entirely, since a live session already proves identity and Supabase doesn't ask for it. `AccountAddresses` moved off the old "patch the whole `user.addresses` array via `updateUser`" pattern onto dedicated `addAddress`/`updateAddress`/`deleteAddress` methods that write to the real `addresses` table, matching how every other context in this codebase (`ProductContext`, `CouponContext`, etc.) already does per-entity mutators rather than whole-array replacement. `Signup.tsx` gained password + confirm-password fields (didn't exist before — mock signup never asked). Both `Login`/`Signup`/`AdminLogin` got a submitting-state guard on their submit buttons, since these are now real network calls instead of synchronous mock writes. `AccountNav`/`AdminSidebar`'s hard-navigation-on-logout now `await`s the now-async `logout()` before the reload, so `signOut()` finishes clearing the session first.

**Two real things had to be fixed on the Supabase project itself, not just in code:**
- **Email confirmation was on by default and had never been pushed off.** `next-app/supabase/config.toml`'s local/dev default already had `enable_confirmations = false` (from `supabase init`), but that's only the *local* config until explicitly synced — confirmed via an isolated `signUp()` test that the remote project still required confirmation (`session: null` back from a successful signup, since there's no SMTP configured to actually deliver a confirmation email). Fixed with `npx supabase config push`, which turned out to target only the Auth and Storage service configs (not `[api]`/`[db]`/etc., confirmed by "Remote API config is up to date" / "Remote DB config is up to date" in its own diff output) — safe to run wholesale. It also tried to push a Storage `vector.enabled` change that 402'd ("upgrade to a paid tier") — harmless no-op, this app doesn't use Storage. Re-tested `signUp()` afterward: session now returns immediately. Worth remembering for later: `site_url`/`additional_redirect_urls` also got pushed to `127.0.0.1` placeholders as a side effect — fine for now (no password-reset/magic-link/OAuth flow exists yet to care), but needs updating to the real deployed domain before any of those ship.
- **`AuthContext.login`/`signup` only awaited the Supabase call itself, not the resulting session hydration** — caught by Playwright, not by reading the diff. Symptom: `Signup.tsx` calls `signup()`, awaits it, then `router.replace('/account')` — but `/account` is behind `ProtectedRoute`, whose `isInitialized` was already latched `true` from the earlier (unauthenticated) `/signup` page load, so it didn't "wait" a second time — it just saw a still-null `user` (the real hydration was happening asynchronously via the `onAuthStateChange` listener, racing against the navigation) and bounced straight back to `/login`. `AdminAuthContext.login` never had this bug, because it already explicitly hydrated `admin` before returning (needed anyway, to do the `admins`-table authorization check). Fixed `AuthContext.login`/`signup` the same way: explicitly `await refreshFromSession(data.session)` before returning success, not just relying on the listener. General lesson, consistent with Wave 2's logout race and Wave 1's SSR bugs: **any auth state change that's immediately followed by a navigation in the same code path needs to be synchronously awaited in the context, not left to an async listener** — the listener alone is fine for state changes that originate elsewhere (another tab, a token refresh), but not for the exact call site that's about to redirect.

Also fixed in passing: `AdminAuthContext`'s `admins`-table lookup used `.single()`, which made Postgres/PostgREST log a `406` to the console every time a signed-in-but-not-admin user got checked (e.g. a customer trying `/admin/login`) — `.single()` throws on zero rows, but "not an admin" is the expected, routine outcome of that specific query, not an exceptional one. Switched to `.maybeSingle()` (returns `null` cleanly on zero rows, still errors on >1). `hydrateUser`'s `profiles` lookup correctly keeps `.single()`, since the `handle_new_user` trigger guarantees exactly one row exists per authenticated user.

Verified with Playwright against `next dev`, driving real Supabase calls (not mocked): signed up a brand-new account end-to-end and landed on `/account`; edited profile name/phone and confirmed the change survived a full page reload (real DB write, not just React state); added an address, confirmed it rendered, confirmed it survived a reload, deleted it; changed password for real, logged out, logged back in with the *new* password, confirmed the *old* password is now rejected; logged in as the seeded customer Ayesha; logged in as the seeded admin; confirmed Ayesha's valid customer credentials are correctly rejected as an admin login (session created, but no `admins` row → signed back out with an error). Zero unexpected console/network errors on the final run (the two real bugs above, plus the `.single()`/406 cleanup, were all caught and fixed *during* this verification pass, same pattern as every prior wave in this project). All throwaway test accounts created during verification were deleted afterward via the Admin API. `tsc`, `eslint` (0 errors, same 17 pre-existing warnings), and `next build` all pass clean.

6c's remaining 6 contexts (`ProductContext`, `CouponContext`, `ReviewContext`, `StoreSettingsContext`, `OrderContext`, `WishlistContext`) are next, and are collectively a much larger phase than the Auth wave — will need their own wave breakdown when requested. `WishlistContext` is worth doing early in that sequence: it's currently a fully disconnected `ids` array (noted back in the 6b plan) that the new `wishlist_items` table can finally fix for real.

Plan docs (sub-phase breakdowns + decisions) live at:
- `C:\Users\SIMPWAE\.claude\plans\start-doing-it-and-luminous-hammock.md` (Phase 5)
- `C:\Users\SIMPWAE\.claude\plans\sharded-stirring-sonnet.md` (Admin panel shell + follow-ups — this file was overwritten between the two planning passes, so it currently reflects the follow-ups plan, not the shell)
- `C:\Users\SIMPWAE\.claude\plans\sharded-noodling-minsky.md` (Phase 6 — this file gets reused/overwritten by each new planning pass in this project too, check its current contents before assuming it still describes Phase 6 rather than whatever was planned most recently)

## 3. Active files

**Phase 5** — created: `src/data/sampleUsers.ts`, `src/context/AuthContext.tsx`, `src/context/WishlistContext.tsx`, `src/components/ProtectedRoute.tsx`, `src/components/account/AccountLayout.tsx`, `src/components/account/AccountNav.tsx`, `src/pages/Login.tsx`, `src/pages/Signup.tsx`, `src/pages/NotFound.tsx`, `src/pages/AccountDashboard.tsx`, `src/pages/AccountOrders.tsx`, `src/pages/AccountOrderDetail.tsx`, `src/pages/AccountWishlist.tsx`, `src/pages/AccountAddresses.tsx`, `src/pages/AccountProfile.tsx`.

**Admin Panel** — created:
- `src/data/sampleAdmins.ts`
- `src/context/AdminAuthContext.tsx`, `ProductContext.tsx`, `CouponContext.tsx`, `ReviewContext.tsx`, `StoreSettingsContext.tsx` (all mirror `OrderContext`'s `useState`-seeded-from-sample-data + mutator-function pattern)
- `src/components/admin/AdminProtectedRoute.tsx`, `AdminSidebar.tsx`, `AdminLayout.tsx`
- `src/pages/admin/`: `AdminLogin.tsx`, `AdminDashboard.tsx`, `AdminOrders.tsx`, `AdminOrderDetail.tsx`, `AdminProducts.tsx`, `AdminProductForm.tsx`, `AdminCustomers.tsx`, `AdminCustomerDetail.tsx`, `AdminCoupons.tsx`, `AdminCouponForm.tsx`, `AdminReviews.tsx`, `AdminSettings.tsx`

Modified: `src/App.tsx` (all providers + routes, now ~15 route entries under `/admin/*`), `src/types/index.ts` (`Admin` type), `src/context/OrderContext.tsx` (`updateOrder`), `src/data/sampleCoupons.ts` (`findValidCoupon` takes a coupons param), `src/components/layout/Header.tsx` (wishlist + product-search wiring), `src/components/checkout/BankTransferPanel.tsx`, `src/pages/Cart.tsx`, `src/pages/Checkout.tsx` (settings/coupon context wiring), `src/pages/Home.tsx`, `CategoryListing.tsx`, `SearchResults.tsx`, `ProductDetail.tsx` (`ProductContext`/`ReviewContext` wiring), `src/components/home/FlashDeals.tsx`, `ProductSection.tsx`, `src/components/listing/ProductGrid.tsx`, `src/components/product/RelatedProducts.tsx` (Phase 5 wishlist prop wiring).

Not touched (deliberately): `src/pages/StyleGuide.tsx` and `src/pages/admin/AdminDashboard.tsx`'s low-stock tile still import `sampleProducts` directly — Phase-1 design-reference page and already-admin-side, both low value to wire.

Also created: `vercel.json` (repo root — SPA catch-all rewrite for the Vercel deployment).

**Follow-up feature batch** — created: `src/pages/admin/AdminDiscounts.tsx`, `src/data/categories.ts`. Modified: `src/context/ProductContext.tsx` (`applyDiscount`), `src/pages/admin/AdminDashboard.tsx` (Sales Overview chart), `src/pages/admin/AdminOrders.tsx` + `AdminOrderDetail.tsx` (customer phone/address display), `src/pages/admin/AdminProductForm.tsx` (now imports `CATEGORIES` instead of defining it locally), `src/components/account/AccountNav.tsx` (`bg-white` fix), `src/components/admin/AdminSidebar.tsx` + `src/App.tsx` (new `/admin/discounts` route + nav entry). `package.json`/`package-lock.json` gained `recharts`.

**Second follow-up batch** — created: `src/data/sampleCoupons.test.ts`, `src/utils/productFilters.test.ts`, `src/pages/admin/AdminDashboard.test.ts`, `src/pages/Checkout.test.ts`. Modified: `src/pages/admin/AdminProducts.tsx` + `AdminCoupons.tsx` (checkboxes, bulk delete, pagination, delete-guard), `src/context/ProductContext.tsx` (`deleteProducts`) + `CouponContext.tsx` (`deleteCoupons`), `src/pages/AccountProfile.tsx` (password-change form), `src/pages/admin/AdminDashboard.tsx` (`groupSales`/`formatBucketLabel` exported + timezone fix), `src/pages/Checkout.tsx` (`validateContact`/`ContactShippingValues` exported), `vite.config.ts` (vitest `test` block, now imports `defineConfig` from `vitest/config` instead of `vite`), `package.json` (`test` script, `vitest` devDependency).

**Phase 6 Wave 1** — entirely new directory, `next-app/`, not touching anything under the repo-root `src/`. Structure: `src/types/`, `src/data/`, `src/utils/`, `src/context/` (all 9, ported ~verbatim + `"use client"` + import fixes), `src/components/` (mirrors the old `src/components/` tree 1:1 — `ui/`, `layout/`, `listing/`, `home/`, `product/`, `checkout/`, `states/`, `track-order/`, plus `CartDrawer.tsx`, `ProductCard.tsx`, `NewsletterModule.tsx`), `src/screens/` (new — `CategoryListingScreen.tsx`, `SearchResultsScreen.tsx`, `TrackOrderScreen.tsx`; holds view components reused across routes or that need a `<Suspense>` wrapper, kept separate from the thin `app/**/page.tsx` files that just wrap them), `src/app/` (13 route folders + `layout.tsx` + `providers.tsx` + `globals.css` + `not-found.tsx`). Config: `next.config.ts` (`turbopack.root` set to silence a monorepo-lockfile warning), `eslint.config.mjs` (one rule disabled, see above), `vitest.config.ts` (separate from `next.config.ts` — Next.js doesn't use Vite, so unlike the old app's `vite.config.ts` this can't double as both).

**Phase 6 Wave 2** — added to `next-app/`: `src/components/ProtectedRoute.tsx`, `src/components/account/AccountLayout.tsx` + `AccountNav.tsx`, `src/app/account/page.tsx` + `orders/page.tsx` + `orders/[id]/page.tsx` + `wishlist/page.tsx` + `addresses/page.tsx` + `profile/page.tsx` (all 6 wrapped in `<ProtectedRoute>`). Modified: `src/context/AuthContext.tsx` (`isInitialized` flag).

**Phase 6 Wave 3** — added to `next-app/`: `src/components/admin/AdminProtectedRoute.tsx` + `AdminLayout.tsx` + `AdminSidebar.tsx`; `src/screens/AdminProductFormScreen.tsx` + `AdminCouponFormScreen.tsx`; `src/app/admin/login/page.tsx`, `page.tsx` + `page.test.ts` (dashboard), `orders/page.tsx` + `orders/[id]/page.tsx`, `products/page.tsx` + `products/new/page.tsx` + `products/[id]/edit/page.tsx`, `discounts/page.tsx`, `customers/page.tsx` + `customers/[id]/page.tsx`, `coupons/page.tsx` + `coupons/new/page.tsx` + `coupons/[code]/edit/page.tsx`, `reviews/page.tsx`, `settings/page.tsx` (all 13 protected pages wrapped in `<AdminProtectedRoute>`, `/admin/login` unwrapped). Modified: `src/context/AdminAuthContext.tsx` (`isInitialized` flag — see §2), `package.json`/`package-lock.json` (`recharts` added).

**Phase 6b** — created: `next-app/supabase/` (CLI-managed: `config.toml`, `migrations/20260714143349_initial_schema.sql`, `seed.sql` — `.temp/` and `.env.local` both gitignored, never committed), `next-app/scripts/seed-auth.mjs`, `next-app/src/lib/supabase/client.ts` + `server.ts`. Modified: `next-app/package.json`/`package-lock.json` (`@supabase/supabase-js`, `@supabase/ssr`, `supabase` CLI added — note the `supabase` CLI package was briefly added to the *repo-root* `package.json` by the user before this session moved it into `next-app/`, where it belongs). No changes to any existing `next-app/` source file — see §2 for why that's deliberate.

**Phase 6c, Wave 1 (Auth)** — modified only, no new files: `next-app/src/lib/supabase/client.ts` (switched from `@supabase/ssr`'s `createBrowserClient` to plain `@supabase/supabase-js`'s `createClient`, now takes a `storageKey` param), `src/context/AuthContext.tsx` + `AdminAuthContext.tsx` (full Supabase Auth rewrite — see §2), `src/app/login/page.tsx`, `src/app/signup/page.tsx` (added password + confirm-password fields), `src/app/admin/login/page.tsx`, `src/app/account/profile/page.tsx` (real password change, dropped the Current Password field), `src/app/account/addresses/page.tsx` (moved to `addAddress`/`updateAddress`/`deleteAddress`), `src/components/account/AccountNav.tsx` + `src/components/admin/AdminSidebar.tsx` (`await logout()` before the hard-navigation reload). Also: pushed an Auth config change to the remote Supabase project itself (`npx supabase config push`, disabling email confirmation) — not a code change, but necessary for real signup to work without SMTP configured, see §2.

## 4. Changes made

See §2 for the section-by-section breakdown. One architectural note not obvious from the file list: **every admin CRUD section is backed by a real React context, not local-only admin state** — this was an explicit user decision (offered as a choice: "fully wired" vs. "admin-only, storefront unchanged") made because a disconnected admin panel would be theater, not a real feature demo.

## 5. Failed attempts / hiccups

- **`git status` initially failed everywhere** ("dubious ownership" — repo owned by a different Windows SID than the current session user) in both the Bash tool and PowerShell. Did **not** fix this with `git config --global --add safe.directory` (that's a persisted config change, off-limits). Used `git -c safe.directory='D:/Projects/alwahab' <command>` instead — a per-invocation flag, not a config write — for every git command this session.
- **Bash tool and PowerShell tool appear to run in separate process namespaces** on this machine: a `npm run dev` backgrounded via Bash was invisible to PowerShell's `Get-CimInstance Win32_Process` (used successfully in the *previous* session to clean up stray vite processes), even though `curl` from Bash could reach it fine on `localhost`. `pkill -f vite` from the Bash tool worked reliably to stop dev servers started from Bash — use that directly instead of the PowerShell/Win32_Process approach next time, contradicting what the previous session's handoff recommended.
- **No browser-automation tool was available by default** this session either. Installed Playwright + Chromium **into the scratchpad directory only** (not the project's `package.json`/`node_modules`) after asking the user for confirmation, since it's a real download/environment change. This let me actually screenshot the app across breakpoints and catch a real mobile-overflow bug (Coupons table delete icon pushed off-screen) that code-reading alone had missed. Worth doing this earlier in future sessions rather than defaulting to "no browser tool available" — ask once, it's a one-time cost.
- Several stray `vite` dev-server processes accumulated across the session (ports 5173–5175) from repeated verification runs; cleaned up with `pkill -f vite` at the end, but worth doing after *every* verification round rather than batching cleanup.
- `.claude/settings.local.json` was untracked and not covered by the existing `.gitignore`'s `*.local` pattern (that pattern only matches files literally ending in `.local`, not `.local.json`). Added `.claude/settings.local.json` specifically to `.gitignore` rather than committing a machine-local file.
- **This machine had no git identity configured at all** (no `user.name`/`user.email`, local or global) — the very first commit attempt failed outright. Did not set it globally; asked the user first, then set it **locally to this repo only** (`git config user.name`/`user.email`, no `--global`) using the email from the session's user context.
- The mobile-sidebar bug in the paragraph above was **not caught by this session's own Playwright pass** — it only surfaced after the user actually deployed and used it on a real phone-sized viewport (or reported based on that). The earlier Playwright screenshots happened to be taken at widths where the overflow was already present but not obviously "broken-looking" enough to flag on sight; the real signal came from measuring `scrollWidth` vs `clientWidth` in the DOM after the user's report, not from eyeballing screenshots. Worth measuring overflow programmatically by default for nav/toolbar-style elements, not just screenshotting them.
- **Follow-up feature batch session**: Playwright wasn't persisted from the prior session (each session gets a fresh scratchpad dir) — Chromium was still cached machine-wide (`~/AppData/Local/ms-playwright`) but the `playwright` npm package itself had to be reinstalled into the new scratchpad. Cheap (Chromium download skipped, browsers already present), confirms the prior handoff's advice to just ask once rather than skip verification.
- **First discount verification attempt gave a false negative**: navigated between admin pages with `page.goto()` to check whether a bulk/category discount had propagated, and the Products page showed unchanged prices — looked like the write hadn't happened. Actually `page.goto()` triggers a full browser navigation, which reloads the SPA from scratch and resets **all** in-memory mock context state (there's no backend/persistence anywhere in this app, by design). Re-ran using an in-app `<Link>` click (client-side route change) instead, which correctly showed the discounted prices. Any future Playwright verification of this app's mock state **must** navigate via clicking in-app links, not `page.goto()`, or every context write will look like it silently failed.
- **Phase 6 Wave 1 session — the `page.goto()`-resets-state lesson above bit again, twice**, and the fixes this time needed to be more surgical since the checkout flow spans multiple pages: (1) after `addToCart`, tried clicking a separate "open cart" button but the drawer was already open (`addToCart` calls `setIsOpen(true)` itself) — a stray toast intercepted the click and timed out; fixed by not clicking to open it at all, just using the drawer that's already there. (2) After placing an order, the plan was to `page.goto('/track-order')` and manually fill the lookup form — but `OrderContext` also isn't persisted, so that `goto()` would've wiped the just-placed order before it could be looked up. Used the order-confirmation page's own pre-filled "Track Order" `<Link href="/track-order?orderId=...&email=...">` instead (in-app client nav). General rule reconfirmed: in this app, *any* multi-step flow that depends on a previous step's in-memory state must stay within one continuous client-side navigation session — no `page.goto()` partway through, regardless of which context the state lives in.
- **Real bugs the migration's stricter build caught that code-reading alone would have missed**: a bare bad-ref-type error (`Header.tsx`), ~2 dozen files missing a `ReactNode`-style import, and a `localStorage`-during-SSR crash were all only found by actually running `next build` and reading the exact error — not by reasoning about the port in the abstract. Worth remembering for Wave 3: budget time for "the stricter toolchain finds pre-existing bugs" as a category, not just "port the code."
- **Phase 6 Wave 2 session**: logging out from a protected page landed on `/login` instead of `/` — traced to `ProtectedRoute`'s reactive redirect racing the explicit post-logout navigation (full explanation in §2). Caught by the Playwright verification pass itself, not by reading the diff. Fixed with a hard navigation on logout; see §2 for why a soft `router.push` couldn't reliably win the race.
- **Phase 6 Wave 3 session — `page.waitForLoadState('networkidle')` and `page.goto(url, { waitUntil: 'networkidle' })` hang forever against `next dev`.** Next's dev server keeps an HMR websocket open indefinitely, so the network never goes idle — the very first Playwright check (a plain `page.goto('/admin')`) timed out at 30s with no other symptom. Switched every `networkidle` wait to `waitUntil: 'domcontentloaded'` for real navigations and dropped `waitForLoadState` entirely after client-side `<Link>` clicks (Playwright's own action auto-waiting — `click()`/`fill()`/`waitForURL()` — already provides the needed synchronization for SPA-style route changes, which don't refire the `load` event anyway). Worth defaulting to `domcontentloaded`/explicit-selector waits from the start for any future Playwright pass against a `next dev` server, rather than reaching for `networkidle` out of Vite-app habit.
- **Phase 6b session — `supabase db push` doesn't apply `seed.sql`.** First push only showed "Schema migrations are up to date" after the seed step was expected; needed `--include-seed` explicitly (`npx supabase db push --include-seed --yes`). Worth remembering: migrations and seed data are two separate opt-in steps against a hosted project, unlike `supabase db reset` for local dev which does both.
- **Phase 6b session — `supabase db query` defaults to the local database, not the linked remote one**, and errors with a Docker-flavored connection failure if Docker Desktop isn't running (`failed to connect to postgres: ... PgClient: Failed to connect`) — misleading, since it looks like a network/credentials problem rather than "wrong target." Needed `--linked` explicitly: `supabase db query --linked "<sql>"`. Also: `supabase db push` itself printed a similar-looking but harmless Docker warning ("failed to cache migrations catalog... in the default daemon configuration on Windows, the docker client must be run with elevated privileges") — that one's safe to ignore, it's just a local caching optimization Docker-less environments skip, not a push failure (the migration still applied — confirmed by row-count queries afterward).
- **Phase 6b session — Postgres RLS silently filters a blocked UPDATE to zero rows instead of throwing an error.** A verification script asserted `!!error` after an anon client tried to update a `products` row protected by an admin-only RLS policy, and the assertion failed — not because RLS was broken, but because the update genuinely succeeded as a no-op (0 rows matched under the policy's `USING` clause) with no error at all. Confirmed the security property actually held by checking the row's value was unchanged. Worth asserting "0 rows affected" (`.select()` after the `.update()` and check `data.length === 0`) rather than "an error was thrown" when verifying RLS blocks a write.
- **Phase 6 Wave 3 session — killing the hung Playwright process needed PowerShell, not Bash.** `pkill` wasn't available in the Bash tool at all this session (contradicting the Wave-1-era handoff note that `pkill -f vite` worked reliably), and `ps aux` from Bash only showed MSYS-level process handles — killing what looked like the right PIDs from Bash left the dev server (and once, a stray Playwright/Chromium process) still bound to its port. `Get-NetTCPConnection -LocalPort <port> | ... OwningProcess` → `Stop-Process -Id <id> -Force` in PowerShell found and killed the actual Windows process reliably. Worth reaching for PowerShell's `Get-NetTCPConnection`/`Stop-Process` first when a Bash-launched dev server or browser process won't die, instead of iterating on `ps`/`kill` from Bash.

## 6. Next steps

- **Confirm the Vercel fix actually works on the live deployment** — push should trigger an auto-deploy if GitHub integration is connected, but this needs an actual click-test against the live URL (not done from this environment, no access to it). Still not confirmed.
- ~~Admin panel gaps: bulk delete, pagination, delete-guard~~ — **done**: Products and Coupons both got checkbox selection, an 8-per-page "Load More", and a guard blocking delete of anything referenced by an existing order.
- ~~No password-change UI~~ — **done** on the customer side (`AccountProfile.tsx`), purely mock/cosmetic since there's still no real credential store. **Still not built on the admin side** — no admin "my profile" page exists yet to hang it on.
- ~~No test suite~~ — **done**: `vitest`, 26 tests (old app) / 21 tests (ported subset in `next-app/`), `npm run test`. Coverage is intentionally narrow (pure functions only — coupon validation, product filter/sort, sales-chart grouping, checkout contact validation). Nothing covers components/contexts/rendering — would need `@testing-library/react` + `jsdom` added if that's wanted next.
- ~~Phase 6 not started~~ — **started, and moving fast**. Full roadmap (6a routing parity → 6b Supabase schema+auth → 6c rewire contexts → 6d cutover) is written into the plan file referenced in §2. **6a and 6b are complete; 6c is underway** (Wave 1/Auth done) — see §2 for the full breakdown. Remaining:
  - **6c, Wave 2+** (next ask): rewire the other 6 contexts (`ProductContext`, `CouponContext`, `ReviewContext`, `StoreSettingsContext`, `OrderContext`, `WishlistContext` — `CartContext` stays mock/local, deliberately, it's ephemeral pre-checkout state) to call Supabase instead of local `useState`/mock arrays. Suggest starting with `WishlistContext`, since it's the one place 6c both migrates *and* fixes a real pre-existing bug (the mock version's `ids` array was never actually connected to the logged-in user). `OrderContext` is probably the most involved (checkout writes an order + order_items transactionally, guest tracking needs to switch from local array lookup to the `find_guest_order` RPC built in 6b).
  - **6d**: cutover — unstarted, blocked on the rest of 6c.
- **No real credential store on the *old* Vite app** (`src/`, not `next-app/`) — that app isn't being migrated, only ported once (6a) then left behind, so its mock auth (`login()` accepts any password) is permanent as-is. **`next-app/` now has a real credential store and it's live** (Supabase Auth, seeded accounts documented in §2, `AuthContext`/`AdminAuthContext` both wired as of 6c Wave 1).
- **Rotate the Supabase `service_role` key and DB password** — both were pasted directly into chat during the 6b session (flagged in the moment, not blocking, but genuinely worth doing): Settings → API → "Reset service_role key", Settings → Database → "Reset password" on the Supabase dashboard. Update `next-app/.env.local` afterward; nothing else needs to change since nothing in committed code reads the old values.
- **Update `site_url`/`additional_redirect_urls` in `next-app/supabase/config.toml` before shipping any password-reset/magic-link/OAuth flow** — the 6c Wave 1 `config push` set these to `127.0.0.1` placeholders (a side effect of pushing the config that actually mattered, disabling email confirmation). Harmless today since no such flow exists yet, but will break email-link redirects once one does, if not updated to the real deployed domain first.
